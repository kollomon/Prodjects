XGS_ENGINE 0.0.1.6 
+ пофиксили некоторые баги (спасибо )

+ написали XGS_converter связаные с работой файлами
    |-позваляет записать значение в файл и потом его считать (более подробно в цикле уроков по движку)
    |-расширение файлов .XGSD (почему везде буквы XGS ? все просто не хватает фантазии =) )
    |-один минус счтитывать в том порядке в котором и написали.
    
-  убрали поддержку TileMapEditor(движок  ругается при загрузке карты =)  в ближайшее время исправим )

-  убрали функции связаные с анимацией (на пркактике оказались бесполензы поэтому щас переписываем .)

XGS_ENGINE 0.0.1.5
+ обработка столкновений 
    |-столкновения обрабатываются если эта карта из TileMapEditor (для остальных пишите сами =(   либо договаривайтесь с нами ,впишем в движок )
 + запихали установщик самого TileMapEditor'а (для чтения карт ничего не нужно  все уже встроено в движок )
XGS_ENGINE 0.0.1.4
+ работа с файлами ( File_EN )
+  поддержка TileMapEditor ( классы Level )
  когда будете ковыряться в исходном коде , не заблудитесь
   (вместо обычных 150 строк 
  там щас целых 500 все из-за 
  поддержки  TileMapEditor и поддержки работы с 
  файлами ), мы раставили коментарии .
  - пока следующие обновы делать не будем (мозгов не хватает, да и тем более нехватает времени на создание игры может выпустим дней через 3-12   )
  
  
XGS_ENGINE 0.0.1.0
+переписали игровой движок ( теперь все в одном классе Object_EN )
  |-меньше кода в игре больше функций в движке.
  |-дабавлено функции для векторов ( расчет угла и т.д )
  |-теперь половина работы игры будет выполнять движок (движение игрока , загрузка текстур и т.д)
 - старые классы полностью убраны .
 
 в процессе :


